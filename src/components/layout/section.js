import React from "react";

import { Flex, Box } from "@chakra-ui/core";

export const PanelSection = ({ children }) => (
  <Flex as="section" width="full">
    <Flex flexWrap="wrap" width="full">
      ca va
    </Flex>
  </Flex>
);
